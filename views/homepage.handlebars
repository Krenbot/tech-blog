<h1>HELLO!</h1>


{{#each blog}}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{title}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{content}}</h3>
  </div>
</div>
{{/each}}